project:
  root: '${name}/'
  executable: ${project.root}bin/${binary.executable}
  zipName: ${name}.tgz
  # ofxLoopinData: bin/data/ofxLoopin

download:
  dir: download/

upload:
  dir: upload/

# http://openframeworks.cc/versions/v0.9.8/of_v0.9.8_osx_release.zip

ofxLoopin:
  releaseURL: http://aws-website-loopintech-ofamp.s3-website-us-east-1.amazonaws.com/bin/${project.zipName}

openframeworks:
  version: 0.10.0
  releaseURL: https://openframeworks.cc/versions/v${openframeworks.version}/${openframeworks.zipName}
  zipPath: download/${settings.openframeworks.zipName}
  root: lib/of_v${settings.openframeworks.version}_${settings.platform}_release


addons:
  ofxKinect:
    enable: true

  ofxOsc:
    enable: true

  ofxPoco:
    enable: true

  ofxSnappy:
    git: https://github.com/arturoc/ofxSnappy

  ofxNice:
    git: https://github.com/koopero/ofxNice

  ofxDepthStreamCompression:
    git: https://github.com/koopero/ofxDepthStreamCompression

  ofxXMPP:
    git: https://github.com/koopero/ofxXMPP

  ofxLoopin:
    packageLink: ./ofxLoopin


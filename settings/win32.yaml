openframeworks:
  zipName: of_v${openframeworks.version}_vs2017_release.zip
  projectGenerator: projectGenerator-vs/resources/app/app/projectGenerator.exe

# binary:
  # executable: ${name}.app/Contents/MacOS/${name}
  # package: ${name}.app


make: msbuild.exe
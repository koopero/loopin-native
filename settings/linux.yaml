openframeworks:
  zipName: of_v${openframeworks.version}_${platform}_release.tar.gz
  projectGenerator: apps/projectGenerator/commandLine/bin/projectGenerator
  projectGeneratorPath: apps/projectGenerator/commandLine

binary:
  executable: ${name}
  package: ${name}
